# ==============================================================================
# CONFIGURAÇÕES DO AMBIENTE - Gestão Condominial
# ==============================================================================
# Copie este arquivo para .env e preencha com seus valores

# Modo de desenvolvimento (True/False)
DEBUG=True

# Chave secreta do Django (NUNCA compartilhe esta chave!)
# Gere uma nova chave com: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=sua-chave-secreta-aqui

# Hosts permitidos (separados por vírgula)
# Em produção, coloque apenas seu domínio real
ALLOWED_HOSTS=localhost,127.0.0.1
